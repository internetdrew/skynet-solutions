<nav
  class='bg-neutral-900 text-center py-2 flex items-center justify-center gap-2 sticky top-0 z-10 sm:py-4'
>
  <span class='red-pulse w-2 h-2'></span>
  <a href='/' class='text-lg font-bold text-[#D1D9E6] sm:text-xl lg:text-2xl'>
    Skynet Solutions
  </a>
</nav>

<style>
  nav {
    font-family: 'Orbitron Variable', sans-serif;
  }

  .red-pulse {
    background-color: #ff0000;
    border: 2px solid #ff3333;
    border-radius: 50%;
    box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
    50% {
      opacity: 1;
      box-shadow: 0 0 16px 4px rgba(255, 0, 0, 1);
    }
    100% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
  }
</style>
