<nav
  class='bg-neutral-900 text-center py-4 flex items-center justify-center gap-2 sticky top-0 z-10'
>
  <span class='red-pulse'></span>
  <a href='/' class='text-2xl font-bold text-[#D1D9E6]'> Skynet Solutions </a>
</nav>

<style>
  nav {
    font-family: 'Orbitron Variable', sans-serif;
  }

  .red-pulse {
    width: 10px;
    height: 10px;
    background-color: #ff0000;
    border: 2px solid #ff3333;
    border-radius: 50%;
    box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
    50% {
      opacity: 1;
      box-shadow: 0 0 16px 4px rgba(255, 0, 0, 1);
    }
    100% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
  }
</style>
