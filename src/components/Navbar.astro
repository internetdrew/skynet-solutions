<nav class='bg-neutral-950 py-2 gap-2 sticky top-0 z-10 sm:py-4'>
  <div
    class='flex items-center justify-between max-w-6xl mx-auto 2xl:max-w-8xl'
  >
    <div class='flex items-center gap-2'>
      <span class='red-pulse w-2 h-2'></span>
      <a href='/' class='font-medium text-[#D1D9E6] sm:text-xl'>
        Skynet Solutions
      </a>
    </div>
    <button
      class='bg-neutral-50 px-4 py-2 rounded-md text-neutral-950 font-medium cursor-pointer'
    >
      Get a demo
    </button>
  </div>
</nav>

<style>
  .red-pulse {
    background-color: #ff0000;
    border: 2px solid #ff3333;
    border-radius: 50%;
    box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
    50% {
      opacity: 1;
      box-shadow: 0 0 16px 4px rgba(255, 0, 0, 1);
    }
    100% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
  }
</style>
