<nav class='bg-neutral-950 py-3 gap-2 sticky top-0 z-10 sm:py-4'>
  <div
    class='flex items-center justify-between font-medium max-w-7xl mx-4 xl:mx-auto'
  >
    <div class='flex items-center gap-1'>
      <span class='red-pulse w-2 h-2 rounded-full'></span>
      <a href='/' class='text-[#D1D9E6] text-lg md:text-xl'>
        Skynet Solutions
      </a>
    </div>
    <button
      class='bg-neutral-50 py-3 px-4 rounded-lg text-neutral-950 cursor-pointer text-sm'
    >
      Get a demo
    </button>
  </div>
</nav>

<style>
  .red-pulse {
    background-color: #ff0000;
    border: 2px solid #ff3333;
    box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
    50% {
      opacity: 1;
      box-shadow: 0 0 16px 4px rgba(255, 0, 0, 1);
    }
    100% {
      opacity: 0.7;
      box-shadow: 0 0 8px 2px rgba(255, 0, 0, 0.8);
    }
  }
</style>
